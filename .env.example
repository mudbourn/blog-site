# Database
DATABASE_URL=postgres://syncopated:syncopated@localhost:5432/syncopated

# Admin auth
ADMIN_USERNAME=owner
ADMIN_PASSWORD_HASH=
JWT_SECRET=

# Navidrome (server-side only, never exposed to the browser)
NAVIDROME_URL=https://music.mudbourn.info/navidrome
NAVIDROME_USER=
NAVIDROME_PASSWORD=

# Nextcloud (server-side only, never exposed to the browser)
NEXTCLOUD_URL=https://save.mudbourn.info/nextcloud
NEXTCLOUD_USER=
NEXTCLOUD_PASSWORD=
